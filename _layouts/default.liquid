---
eleventyComputed:
  css: "{{ site.css2 }}"
layout: base
---
<header>
    {% include 'nav-primary' %}
    <h1>{{ site.name }}</h1>
</header>
<main>
    {% if aside %}
    <aside>
        {% include 'nav-secondary' %}
    </aside>
    {% endif %}
    <section aria-labelledby="{{ title | slugify }}">
        <h2 id="{{ title | slugify }}">{{ title }}</h2>
        {% if description %}<p>{{ description }}</p>{% endif %}
        <p>Stylesheet in use: {{ css }}.</p>
        {{ content }}
    </section>
</main>
<footer>
    {% include 'nav-footer' %}
    {% include 'copyright' %}
</footer>